#!/usr/bin/env bash
# This script displays a message indefinitely and handles SIGTERM

invincible_response() {
    printf "I am invincible!!!\n"
}

# Trap the SIGTERM signal (15) to execute the function
trap 'invincible_response' SIGTERM

while true; do
    echo "To infinity and beyond"
    sleep 2
done
